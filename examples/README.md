# Using the Makefile in here

The makefile has a modifiable value: SRC.

Simply enough, specify what file you wish to compile, and there you go. The final application will have a name: `minigfx-compiled`

```
$ make SRC=window.c
```